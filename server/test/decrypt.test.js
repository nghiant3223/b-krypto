import { aesFolderEncrypt } from '../services/encryption.service';
import { aesFolderDecrypt } from '../services/decryption.service';

// aesFolderEncrypt('template (copy)', 'key.txt');
// aesFolderDecrypt('template (copy)', 'key.txt');
